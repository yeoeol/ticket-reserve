server:
  port: 30010

spring:
  application:
    name: admin-service

  mvc:
    hidden-method:
      filter:
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/